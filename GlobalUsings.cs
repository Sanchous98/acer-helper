// Project-wide usings for the shared Domain layer (model + feature ports), so files don't each
// repeat them. Application/Infrastructure/UI all speak this language; OS- and vendor-specific
// namespaces are imported explicitly where needed.

global using AcerHelper.Features;
