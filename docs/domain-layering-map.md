# Карта слоёв: всё, что объявлено под `Domain/`

Составлена **2026-09-17** по директиве владельца. Это **разбор, а не работа**: ни один
продакшн-файл и ни один тест этим документом не тронут. Задача — провести правило
владельца по **всем** типам домена один раз, чтобы правки не шли по одному типу за раз.

**Как получен список.** Типы перечислены из файлов, а не из плана и не по памяти: 15
файлов под `Domain/` прочитаны целиком, объявления собраны по разметке
(`class`/`record`/`struct`/`interface`/`enum`). Их **69** — включая перечисления,
интерфейсы и статические классы, вложенных типов нет. Три прежних разбора в этой же
сессии называли тип, которого нет, читателя, которого нет, и неверное число; поэтому
число здесь проверяемо: `grep -nE '^\s*(public|internal|private|protected)?\s*(static\s+)?(sealed\s+|abstract\s+|readonly\s+|partial\s+)*(class|record|struct|interface|enum)\s' Domain/*.cs`.

---

## Правило, по которому раскладывается каждый тип

> «В домене живут модели, отвечающие за логику приложения. Именно логику! А интеграция
> с железом живет в инфраструктуре. Набор действий — это use cases, то есть application
> уровень»

- **Домен** — модели, несущие логику приложения.
- **Application** — use case: набор действий, оркестрация моделей и контрактов.
- **Инфраструктура** — интеграция с железом, включая форму хранения `settings.json` и
  **опции, которые объявляет конкретно этот ноутбук**.

### Чего правило не покрывает: интерфейсы и чистые значения

Правило говорит о **моделях, несущих логику**, а под `Domain/` лежат ещё две категории,
которые под него буквально не подпадают.

**Первая — контракты: 21 интерфейс.** Интерфейс не несёт ничего, поэтому «где логика» к
нему не применимо, и размещать его по букве правила нельзя. Ниже применён тот же приём,
которым владелец уже разрешил `Settings`: **типы-объявления — домен, экземпляры приходят
из бэкендов.** Из него и из машинной проверки `ArchitectureMapTests` (домен не смотрит
вверх) следует различение, которое здесь применяется ко всем контрактам:

- контракт, **который называет доменный объект** или который говорит только на
  нейтральном словаре домена, — остаётся в домене;
- контракт, который **моделирует технологию вендора или ОС** (`ICurveOptimizer` — AMD,
  `IGpuOverclock` — Nvidia, `ICpuPower` — оверлей Windows, `IKeyboardBrightness` — уровни
  Dell), — интеграция, и владелец уже вынес так `VoltageDomain`/`CoAxis`.

Второе следствие этого различения — **`IDevice`**: он перечисляет и те, и другие, поэтому
он один держит весь вендорский блок портов. Это вынесено в «Спорные» отдельным пунктом,
потому что здесь правило действительно не решает, а цена ошибки — вся карта.

**Вторая — чистые значения.** `AccentColor`, `FanReading`, `BatteryState`, `ChoiceOption`
своей логики не несут: это **словарь**, на котором записаны правила (заголовок
`Domain/Models.cs` называет их именно так — «ubiquitous language… vendor- and OS-agnostic
value objects»), и глоссарий держит их в домене. Требование «для домена назвать логику» к
ним применимо только в ослабленной форме, и ниже она применена честно: у таких типов
названо **правило, которое над ними записано**, либо прямо сказано, что своей логики нет.
Притворяться, что `AccentColor` несёт логику, было бы хуже, чем назвать его тем, что он
есть.

### Что не переоткрывается

Позиции владельца приняты как данные: `AppArgs` → Application; `LampArray` /
`LampArrayBridge` → Инфраструктура; `Settings` (контейнер) → Инфраструктура, но его
контрактная половина остаётся в домене; `ModeKey` — не домен; `CoAxis` / `CoPreset` /
`OffsetCounts` / `VoltageDomain` — в основном инфраструктура; `Fan` не знает, что он
GPU-вентилятор; расписание переприменения (`ModeAxisTable.Schedule`, `ReapplyTrigger`) →
Application, а политики и волатильность (`EmptyAxisPolicy`, `IsVolatile`, `Owner`) — домен.

---

## 1. Разбор по файлам

Обозначения в колонке «цена»: **JSON-имя** — персистентное имя под стражем на фикстуре;
**глоссарий** — `GlossaryTests`; **карта** — `ArchitectureMapTests`; **контракт** — перенос
задевает порт и потому бэкенды и фейки; **переезд** — только файл, namespace и `using`-и.

### `Domain/AppArgs.cs`

| Тип | Что это сейчас | Слой | Почему | Цена |
|---|---|---|---|---|
| `AppArgs` | Внутренняя константа `--startup`, общая для регистрации автозапуска и разбора аргументов | **Application** | Решено владельцем: CLI-константа, к предметной области отношения не имеет | **Переезд.** `internal`, 3 потребителя (`Autostart.Windows.cs`, `Autostart.Linux.cs`, `UI/App.axaml.cs`). Не JSON, не глоссарий, `IDevice` не задет. У `App.axaml.cs` уже есть `using AcerHelper.Application;` — правки только в двух файлах автозапуска |

### `Domain/Battery.cs`

| Тип | Что это сейчас | Слой | Почему | Цена |
|---|---|---|---|---|
| `Battery` | Батарея этой машины; наличие каждого свойства объявляет сам член (`Telemetry`, `ChargeLimit`, `Calibration`, `ChargeMode`) | **Домен** | Логика: **что такое батарея** — её форма отвечает на вопрос «что эта батарея умеет», а `Read()` сворачивает отсутствие телеметрии в «неизвестный» снимок, то есть отказ и отсутствие различает одно место. Экземпляр собирают бэкенды — ровно тот приём, которым разрешён `Settings` | Не переезжает. **Контракт:** `IDevice.Battery` ненулевой, поэтому слот `IDevice` задет, если двигать |
| `BatteryToggle` | Пара операций «прочитать/записать» с обеими половинами исхода | **Домен** | Логика: **чем свойство пишется** — отказ приходит из самого вызова, а не читается из чужого поля после него (docs/open-decisions.md §2). Форма та же, что у держателей `DelegatePorts` | Не переезжает. Закреплено волной 4b (принято владельцем) |
| `BatteryChoice` | То же для свойства «один из N» вместе с набором вариантов прошивки | **Домен** | То же: набор вариантов — часть формы свойства, а не транспорта | Не переезжает |

### `Domain/CoAxis.cs`

| Тип | Что это сейчас | Слой | Почему | Цена |
|---|---|---|---|---|
| `CoAxis` | Четыре правила оси Curve Optimizer: кламп на рельс, развилка «по рельсам или одно значение», слот по ключу рельса, сторож «ни один режим не настраивали» | **Инфраструктура** | Решено владельцем: правила привязаны к Curve Optimizer, а это чистый AMD. Тест владельца — «а как насчёт Intel XTU?»: кламп AVFS-счётчиков по рельсам моделирует вендора, а не задачу | **Переезд** + перенос двух тестов (`CoAxisTests`, половина `FanTests`). Не JSON, не глоссарий, `IDevice` не задет — значит **выполнимо независимо**: `CoAxis` называет `VoltageDomain`/`OffsetCounts`/`CoPreset`/`ModeAxisTable`, а инфраструктуре смотреть в домен можно. Двигать вместе с `OffsetCounts` |

### `Domain/Fan.cs`

| Тип | Что это сейчас | Слой | Почему | Цена |
|---|---|---|---|---|
| `Fan` | Один вентилятор: анкоры, дефолтная кривая, интерполяция, память о последнем применённом дежурстве. Вид на свою половину `FanPreset` | **Домен** | Логика: **как вентилятор ведёт свою скорость** — что такое кривая, как температура превращается в дежурство, что делать при нечитаемой температуре, и что помнить о последней записи. Вид, а не хранимый тип, — это замороженная схема, а не отложенное решение | Не переезжает. `FanCurveEngineStepTests` не правился ни на строку при волне 3 — доказательство, что контракт пережил перенос кривой в модель |

### `Domain/FanCurveEngine.cs`

| Тип | Что это сейчас | Слой | Почему | Цена |
|---|---|---|---|---|
| `FanCurveEngine` | Пара вентиляторов и **парный** дедбенд: решает, писать ли вообще, и отдаёт обе скорости. Портов не касается | **Домен** | Логика: **правило пары** — дедбенд решается на обоих вентиляторах сразу, потому что порт пишет пару (`IFanControl.SetCustomSpeeds`); плюс память гистерезиса, из-за которой отказанная запись повторяется на следующем шаге, а не глотается. Состояние без I/O — это модель, а не действие: применять ходит вызывающий | Не переезжает |

### `Domain/LampArray.cs` — 8 типов

| Тип | Что это сейчас | Слой | Почему | Цена |
|---|---|---|---|---|
| `LampArrayKind` | Вид устройства для отчёта атрибутов HID | **Инфраструктура** | Значения — из спецификации HID, не наши | **Переезд** (весь блок ниже — одна работа) |
| `LampPurposes` | Назначение лампы, битовые значения спецификации | **Инфраструктура** | То же | **Переезд** |
| `LampColor` | Цвет лампы с каналом интенсивности, как его шлёт хост | **Инфраструктура** | Форма кадра на проводе | **Переезд** |
| `LampInfo` | Атрибуты одной лампы в микрометрах и микросекундах | **Инфраструктура** | Единицы провода; перевод мм/мс делается здесь же | **Переезд** |
| `LampTarget` | Куда лампа ложится в модели зон: зона + подзона | **Инфраструктура** | Это карта обхода для моста, а не свойство зоны | **Переезд** |
| `LampFrame` | Полный кадр от хоста + признак автономного режима | **Инфраструктура** | Сообщение транспорта | **Переезд** |
| `ILampArrayTransport` | Транспорт, делающий макет видимым ОС как HID-устройство | **Инфраструктура** | Решено владельцем: API для управления подсветкой — транспорт плюс воркер | **Переезд.** Реализуется двумя файлами `Vendors/Generic/LampArrayTransport.{Windows,Linux}.cs` — они уже под `Infrastructure/`, и `*.Windows.cs`/`*.Linux.cs` выше инфраструктуры запрещены картой |
| `LampArrayLayout` | Макет: габарит, вид, лампы, цели; `Build(IRgbDevice)` строит его из зон | **Инфраструктура** | Геометрия и правило раскладки — описание того, как устройство выглядит для хоста | **Переезд.** Зовёт `IRgbDevice`/`RgbZone` — инфраструктуре в домен можно |

### `Domain/LampArrayBridge.cs`

| Тип | Что это сейчас | Слой | Почему | Цена |
|---|---|---|---|---|
| `LampArrayBridge` | Транспорт + воркер: разбирает частоту, дедуплицирует записи и арбитрирует владение подсветкой | **Инфраструктура** | Решено владельцем прямо: транспорт плюс воркер | **Переезд + контракт.** 5 продакшн-потребителей: `LaptopService` (ctor и поле), `LaptopService.Lighting.cs`, `OptionsAssembler`, `DeviceFactory`, два вендорских файла; 3 тестовых файла и 2 фейка. Не JSON, не глоссарий, `IDevice` не задет. **Дорогое место — `Application` начнёт называть тип инфраструктуры** в сигнатуре (`LaptopService(`…, `ILampArrayTransport?`)`, `svc.LampArray`, `OptionsAssembler`). Лечится тем, что Application объявляет узкий контракт на нужную ему часть (`Enable`/`Disable`/`HostOwnsLighting`/`OwnerChanged`/`Reassert`/`LastError`), а мост его реализует; это отдельное решение, а не часть переезда |

### `Domain/ModeAxis.cs`

| Тип | Что это сейчас | Слой | Почему | Цена |
|---|---|---|---|---|
| `ModeAxis` | Перечисление осей, у которых есть пресет на режим; список и есть перечисление | **Домен** | Логика: **набор осей, который несёт режим** — шестая ось добавляется здесь и больше нигде | Не переезжает. **Глоссарий, жёстко:** имя обязано резолвиться и стоять в `docs/context-map.md` |
| `EmptyAxisPolicy` | Что делать, когда у текущего режима нет пресета: оставить / выставить сток / унаследовать вид | **Домен** | Решено владельцем. Логика: **что делать с осью, у которой нет пресета** — три ответа, каждый вынужден железом (значение нечитаемо; сток достоверен; ось write-only) | Не переезжает. **Глоссарий, жёстко** |
| `ReassertOwner` | Кто переприменяет ось после того, как платформа её забыла: железо или UI | **Домен** | Решено владельцем. Логика: **кто обязан переприменить ось** — подсветка единственная ось, чей повтор живёт в UI | Не переезжает. **Глоссарий, жёстко** |
| `ReapplyTrigger` | Что просит вернуть волатильное состояние: старт, смена режима, пробуждение | **Application** | Решено владельцем: триггер — факт о **расписании**, а расписание есть набор действий, то есть use case | **Переезд + переименование члена.** 4 продакшн-потребителя (`HardwareReconciler`, `LaptopService`, `AppController`, `LightingCoordinator`) и 3 тестовых. Не в жёстком списке глоссария; в `context-map.md` §3 «триггер» намеренно без типа. **Контракт:** `Schedule(ReapplyTrigger)` переезжает вместе с ним |
| `ModeAxisTable` | Таблица пяти осей как данных: `All`, `Schedule`, `Policy`, `PolicyWhenNoModeWasEverConfigured`, `IsVolatile`, `Owner` | **Домен — минус `Schedule`** | Логика (остаётся): **какие оси волатильны** и **политика оси без пресета** — решено владельцем. **`Schedule` — use case** и уходит в Application: «какие оси, в каком порядке» для операции, которая их применяет | **Разрезание члена.** `Schedule` читает `HardwareReconciler`; зовут его 3 теста. Разрез вынужден границей слоёв: пока `Schedule` и `ReapplyTrigger` в домене, домен объявляет расписание, а владелец отдал расписание Application. **Глоссарий, жёстко** — имя типа остаётся, поэтому либо таблица остаётся одна и теряет `Schedule`, либо расписание получает второй тип в Application; это решение об именах, а не о слоях |

### `Domain/ModeKey.cs`

| Тип | Что это сейчас | Слой | Почему | Цена |
|---|---|---|---|---|
| `ModeKey` | Идентичность режима, под которой лежат пресеты: `None`/`From`/`For` | **Инфраструктура** | Решено владельцем: не домен — ключ словаря для раскладки пресетов по режимам есть дело хранения. Домену важно, что профиль **может** смениться, а не то, как он меняется. Дом ничего не зовёт: `For`/`From` — половины того же значения, и ни один доменный файл `ModeKey` не называет | **Переезд** рядом с `Settings` (ключ персистится как ключ словаря в `settings.json`). **Глоссарий, жёстко** — имя обязано резолвиться и стоять в `context-map.md`; строка «Domain» в его колонке слоя станет неверной. Потребителей мало: один продакшн (`LaptopService.Profiles.cs`) и два тестовых (`ModeKeyTests`, `GlossaryTests`) |

### `Domain/Models.cs` — 12 типов

| Тип | Что это сейчас | Слой | Почему | Цена |
|---|---|---|---|---|
| `AccentColor` | Нейтральный RGB-триплет без зависимости от тулкита | **Домен** | Своей логики нет — это слово, которым домен говорит о цвете: правило «зона может следовать за профилем» и «профиль несёт акцент и цвет вспышки» записаны поверх него, а `LampColor.Rgb` сводит к нему провод | Не переезжает. **Глоссарий** |
| `ProfileKind` | Грубый класс профиля | **Домен** | Логика: **чем профили отличаются по классу** — цвет иконки и семантика «переключить производительность» без имени вендора | Не переезжает. **Глоссарий** |
| `PerformanceProfile` | Профиль как его видит приложение: непрозрачный `Id`, имя, класс, цвета | **Домен** | Логика: **намерение пользователя** — какой режим выбран. Не аппаратный конверт мощности | Не переезжает. **Глоссарий** |
| `FanMode` | Поведение вентилятора: Auto / Max / Custom | **Домен** | Логика: **какие поведения у вентилятора бывают** — с ними домен и рассуждает; значения совпадают с WMI-значениями Acer, но словарь нейтрален | Не переезжает. **Глоссарий.** См. «Спорные»: числа `1/2/3` — это кодировка Acer, и `FanPreset.Mode` персистит именно их |
| `FanCapability` | Что предлагает управление вентиляторами (Auto подразумевается) | **Домен** | Словарь домена: **что такое возможность управления вентилятором**. Кодировки вендора внутри нет | Не переезжает; потребители — Application и UI |
| `FanReading` | Показание одного вентилятора; `Rpm = -1` значит «нечитаемо» | **Домен** | Своей логики нет — слово, которым `SensorSnapshot` говорит об одном вентиляторе; правило «`-1` значит нет» записано поверх него | Не переезжает |
| `SensorSnapshot` | Живые температуры и обороты; `-1` значит «нет» | **Домен** | Логика: **по чему считается дежурство** — против этого снимка вычисляется кривая (`Fan.Duty`), и «нечитаемая температура держит последнее дежурство» — правило домена над этой формой | Не переезжает. **Глоссарий** |
| `HotkeyAction` | Обобщённые действия спецклавиши | **Домен** | Логика: **что вообще может значить спецклавиша** — бэкенд отображает свои клавиши в этот словарь, а что каждое действие делает, решает приложение | Не переезжает |
| `RgbModeInfo` | Описание эффекта подсветки + непрозрачный `Handle` с вендорской кодировкой | **Домен** | Логика: **что за эффект предлагается** — имя и признаки (цвет/скорость/направление), по которым UI решает, показывать ли палитру. `Handle` — вендорский payload внутри домена; см. «Спорные» | Не переезжает: `IRgbDevice.Zones` его называет, а `IRgbDevice` — слот `IDevice`. **Контракт:** 3 тестовых файла читают `HasColor`/`HasSpeed` |
| `ChoiceOption` | Один именованный вариант с устойчивым id | **Домен** | Своей логики нет — словарь, на котором говорят `BatteryChoice`, `ICpuPower` и `IChoicePort`; правило «id устойчив, имя показывается» записано поверх него | Не переезжает. **Глоссарий** |
| `BatteryState` | Состояние заряда/разряда | **Домен** | Своей логики нет — часть формы показаний батареи, над которой записано «`Percent = -1` значит нет» | Не переезжает |
| `BatteryInfoSnapshot` | Живые показания батареи; `-1` значит «нет» | **Домен** | Логика: **что такое показание батареи**, включая «процентов нет» как отдельное значение; `Battery.Read()` сворачивает в него отсутствие телеметрии | Не переезжает. **Глоссарий** |

### `Domain/Options.cs`

| Тип | Что это сейчас | Слой | Почему | Цена |
|---|---|---|---|---|
| `OptionToggle` | Строка-переключатель: подпись, доступность, значение, запись, чтение-сверка, подтверждение, прайм | **Спорные** | Правило не решает: это **не модель** (логики нет), **не use case** (ничего не выполняет) и **не интеграция** (операции приходят извне). Это форма строки, которую собирает Application и рисует UI. См. «Спорные» | См. «Спорные» |
| `OptionChoice` | То же для строки-выбора | **Спорные** | То же | См. «Спорные» |

### `Domain/Ports.cs` — 16 типов

| Тип | Что это сейчас | Слой | Почему | Цена |
|---|---|---|---|---|
| `IPowerProfiles` | Контракт переключения профилей | **Домен** | Тот самый предмет, ради которого домен существует: **что профиль можно переключить** и какие из них доступны сейчас. Словарь (`PerformanceProfile`, `ProfileKind`) — доменный | Не переезжает |
| `IFanControl` | Контракт записи режима и пары скоростей | **Домен** | Контракт оси вентиляторов: `FanMode`/`FanCapability` — доменный словарь, и порт называет способ записи ровно тем, что домен решил | Не переезжает |
| `ISensors` | Контракт чтения телеметрии | **Домен** | Контракт, через который приходит `SensorSnapshot`, против которого считается кривая | Не переезжает |
| `IFlagPort` | Форма булева транспорта настройки (чтение/запись/ошибка) | **Домен** | **Пришпилен:** `FlagSetting.Port` — часть контрактной половины `Settings`, а её владелец оставил в домене. Перенос порта сделал бы доменную декларацию зависящей от инфраструктуры, то есть невыполнимым | Не переезжает. Альтернатива названа в «Спорных» |
| `IChoicePort` | То же для «один из N» | **Домен** | То же, через `ChoiceSetting.Port` | Не переезжает |
| `IKeyboardBrightness` | Яркость обычной (не-RGB) подсветки в дискретных аппаратных уровнях | **Инфраструктура** | Моделирует уровни вендора (у Dell 0..2 = выкл/тускло/ярко) — технологии, а не задачу. Ни один доменный объект о ней не рассуждает | **Гейт `IDevice`** — см. ниже |
| `IHotkeys` | Спецклавиши, отображённые в обобщённые действия | **Инфраструктура** | Транспорт ввода + события; домен о нём не рассуждает, `HotkeyAction` остаётся доменным | **Гейт `IDevice`** |
| `IDisplayTint` | Синий фильтр через гамму | **Инфраструктура** | API дисплея; уровней и гаммы домен не знает | **Гейт `IDevice`** |
| `IGpuOverclock` | Разгон dGPU: офсеты ядра и памяти в МГц | **Инфраструктура** | Тот же тест владельца: моделирует NvAPI-офсеты Nvidia. «А если GPU не Nvidia?» — вопрос того же класса, что «а как насчёт Intel XTU» | **Гейт `IDevice`** |
| `ICpuPower` | Оверлей питания Windows: три схемы по GUID | **Инфраструктура** | Интеграция с API ОС: id — GUID схем оверлея, и это не словарь домена | **Гейт `IDevice`** |
| `ICurveOptimizer` | Андервольт AMD Curve Optimizer в AVFS-счётчиках | **Инфраструктура** | Решено владельцем для всего CO-блока. Порт несёт вендорскую модель (`VoltageDomain`) и не может уехать без неё | **Гейт `IDevice` + гейт `VoltageDomain`** |
| `VoltageDomain` | Независимо настраиваемый рельс напряжения: `Label`, `Key`, `Range`, `MillivoltsPerCount` | **Инфраструктура** | Решено владельцем («в основном инфраструктура»): рельс с милливольтами на счётчик — измеренное свойство конкретного кремния AMD | **Гейт `ICurveOptimizer`.** Не переносится, пока `ICurveOptimizer` в домене: `ICurveOptimizer.Domains` возвращает `IReadOnlyList<VoltageDomain>`, и переезд в одиночку покрасил бы `DomainPointsAtNothingButItselfAndLocalization` |
| `IDriverSetup` | Предложение установить сторонний драйвер | **Инфраструктура** | Интеграция с чужим ПО: установка драйвера — действие пользователя над чужим продуктом, логики домена внутри нет | **Гейт `IDevice`** |
| `IAutostart` | Управление автозапуском | **Инфраструктура** | Служба ОС (реестр, `.desktop`), домен о ней не рассуждает | **Гейт `IDevice`** |
| `IClamshell` | Не давать машине спать при закрытой крышке | **Инфраструктура** | Служба ОС + питание; логики домена внутри нет | **Гейт `IDevice`** |
| `IDevice` | Подключённый ноутбук: набор ненулевых портов и есть модель возможностей | **Спорные** | Правило не решает, и это **ключевой вопрос карты**: см. «Спорные». От него зависит, переносится ли весь вендорский блок портов выше | См. «Спорные» |

**«Гейт `IDevice`»** значит: тип — член `IDevice`, который сам остаётся в домене, поэтому
перенос в одиночку делает доменный интерфейс зависящим от инфраструктуры. Это не мнение,
а машинная проверка: `ArchitectureMapTests.DomainPointsAtNothingButItselfAndLocalization`
покраснеет. Переносить их можно только **одним блоком с `IDevice`**, и это решение
владельца, а не работа.

### `Domain/Rgb.cs`

| Тип | Что это сейчас | Слой | Почему | Цена |
|---|---|---|---|---|
| `RgbZone` | Управляемая зона: список эффектов, подзоны, операции записи, «следует за профилем» | **Домен** | Логика: **что такое зона подсветки** и что означает «эта зона следует за профилем» — по этому признаку UI не строит панель и не шлёт в зону ничего. Операции — делегаты, ровно как у принятой владельцем `Battery` | Не переезжает: `IRgbDevice.Zones` его называет, а `IRgbDevice` — слот `IDevice`. **Контракт:** 11 файлов вне домена |
| `IRgbDevice` | Поверхность подсветки устройства — порт возможности | **Домен** | Контракт, который называет `IDevice.Lighting`; доменного словаря (`RgbZone`, `AccentColor`, `RgbModeInfo`) | Не переезжает |
| `IRgbController` | Транспорт, порождающий зоны, которые умеет вести (ENE HID, будущий LampArray) | **Инфраструктура** | Его собственный доккомментарий называет его транспортом. `IDevice` его не перечисляет, поэтому **выполнимо независимо** | **Переезд.** Потребители: вендорские бэкенды и 4 тестовых файла/фейка. Не JSON, не глоссарий |
| `RgbDevice` | Сборка `IRgbDevice` из контроллеров и владение ими | **Инфраструктура** | Композиция транспортов и `Dispose` — интеграция. `IDevice` его не называет — **выполнимо независимо** | **Переезд**, тот же блок, что `IRgbController` |

### `Domain/Settings.cs` — 12 типов

| Тип | Что это сейчас | Слой | Почему | Цена |
|---|---|---|---|---|
| `Settings` | Граф настроек: слоты источников, пять словарей пресетов, скаляры, мешок вендорских значений | **Инфраструктура** | Решено владельцем: объявляет опции **конкретно этого ноутбука** и является формой хранения. Заодно уходит `using AcerHelper.Localization` — и «единственное исключение» из `ArchitectureMapTests` перестаёт существовать | **Переезд** в `Infrastructure/Composition/` рядом с `JsonSettingsStore`. **JSON:** имя **типа** в файле не участвует, поэтому переезд имя-безопасен; персистятся имена **свойств**. **карта:** namespace обязан стать именем папки. **Потребители — восемь**, и это дешевле, чем читается: 7 файлов Application (все читают граф через `svc.Settings`, то есть имени типа не пишут) и `JsonSettingsStore`. UI к типу не обращается вовсе — четыре файла, где встречается слово, упоминают его в комментариях. **Контракт:** тянет за собой `ISettingsStore`; `JsonSettingsStore` не меняется кроме namespace, `FakeSettingsStore` получает `using` |
| `SettingDeclaration` | Форма объявления настройки: непрозрачный `Key`, `ReadbackVerifiesWrite`, `Apply` | **Домен** | Решено владельцем: типы-объявления остаются в домене. Логика: **что значит «настройка объявлена»** и **что значит «её применить»** — отказ есть исключение с информацией и без имени настройки | Не переезжает. **Контракт:** держит `IFlagPort`/`IChoicePort`; `<see cref="Settings.DeviceSettings"/>` в доккомментарии придётся переписать на полное имя или снять, иначе cref не разрешится (в проекте нет `TreatWarningsAsErrors`, поэтому это предупреждение, а не ошибка сборки) |
| `FlagSetting` | Объявление булевой настройки: значения `"1"`/`"0"`, бросок транспорта как отказ | **Домен** | Решено владельцем; логика: **кодировка флага и сворачивание броска транспорта в отказ** — одна на всех | Не переезжает |
| `ChoiceSetting` | Объявление выборной настройки + отображение id↔индекс | **Домен** | Решено владельцем; логика: **где живёт отображение id в индекс**, которое нужно строке | Не переезжает |
| `SettingNotAppliedException` | Отказ применения: ключ + причина транспорта, без текста для пользователя | **Домен** | Решено владельцем (волна 9): домен бросает, UI составляет фразу. Логика: **что несёт отказ** — информацию, а не сообщение | Не переезжает |
| `LightSettings` | Состояние одной зоны: эффект, яркость, скорость, направление, цвет, цвета подзон | **Инфраструктура** | Форма хранения: `settings.json`, словарь зон одного пресета | **Переезд** с `Settings`. **JSON** — `Zones`, `Configured`, `EffectIndex`, `Brightness`, `Speed`, `Direction`, `Color`, `ZoneColors` под стражем на фикстуре; переезд имя-безопасен, переименование свойства требует `[JsonPropertyName]`. **Контракт:** 6 продакшн-файлов (Application и UI) |
| `ProfileMemory` | Режим, запомненный за источником питания: база + флаг Turbo | **Инфраструктура** | Форма хранения — записано в плане (§3, волна 7): «`ProfileMemory` не переехал и переезжать не должен: это форма ХРАНЕНИЯ». Перевозить сейчас нечего — правило уже уехало в `LaptopService.ModeKeyFor` | **Переезд** с `Settings`. **JSON** — `BaseId`, `Turbo` защищены **только** фикстурой. 5 тестовых файлов |
| `FanPreset` | Пресет кулеров одного режима: режим, две фиксированные скорости, две кривые, `Snapshot()` | **Инфраструктура — но перенос НЕВЫПОЛНИМ сейчас** | По правилу это форма хранения (замороженная схема, держит **оба** вентилятора в одном объекте) — то есть инфраструктура. Но `Fan.Duty(FanPreset, …)` и `FanCurveEngine.Step(FanPreset, …)` — доменные, и перенос делает домен зависящим от инфраструктуры | **Заблокирован `Fan`.** См. «Спорные»: либо пресет остаётся доменным DTO, на половину которого смотрит модель, либо меняется сигнатура `Fan` — а это смена контракта, и в эту карту она не входит. **JSON** — `Mode`, `Cpu`, `Gpu`, `CpuUseCurve`, `GpuUseCurve`, `CpuCurve`, `GpuCurve` под стражем |
| `LightPreset` | Подсветка одного режима, по зонам | **Инфраструктура** | Форма хранения; никто в домене её не называет | **Переезд** с `Settings`. **JSON** — `Zones` |
| `GpuOcPreset` | GPU-офсеты одного режима в МГц + `Snapshot()` | **Инфраструктура** | Форма хранения; домен её не называет (правил у GPU-половины нет — замерено волной 6) | **Переезд** с `Settings`. **JSON** — `Core`, `Mem`. 6 продакшн-потребителей, включая UI |
| `CoPreset` | Смещения Curve Optimizer одного режима: `AllCore` + `Domains` по ключам рельсов | **Инфраструктура** | Решено владельцем («в основном инфраструктура») — и это ещё и форма хранения | **Переезд** с `Settings`, независимо от `CoAxis`: домен её не называет. **JSON** — `AllCore`, `Domains` и **ключи внутри** (`ccd:0`, `gfx` — значения `VoltageDomain.Key`) |
| `ISettingsStore` | Порт сохранения графа настроек: `Load`/`Save` | **Инфраструктура** | Его сигнатура называет `Settings`, поэтому он обязан переехать с ним: иначе доменный порт зависел бы от инфраструктуры. Сам по себе это порт **хранилища**, а не контракт домена | **Переезд** с `Settings` в `Infrastructure/Composition/`. 3 потребителя (`JsonSettingsStore`, `LaptopService`, `FakeSettingsStore`). **Контракт:** `LaptopService`'s ctor начнёт называть тип инфраструктуры — тот же вопрос, что у `ILampArrayTransport` |

### `Domain/Values.cs`

| Тип | Что это сейчас | Слой | Почему | Цена |
|---|---|---|---|---|
| `OffsetCounts` | Смещение CO в AVFS-счётчиках; овервольт непредставим (`Clamp`) | **Инфраструктура** | Решено владельцем («в основном инфраструктура»): инвариант «только андервольт» — про AVFS-счётчики AMD, а не про задачу. Единственный потребитель в домене — `CoAxis`, который едет вместе | **Переезд** вместе с `CoAxis`. Не JSON. **Глоссарий, жёстко:** имя обязано резолвиться и стоять в `context-map.md` — переезд это выдерживает (резолв по простому имени в сборке), переименование нет |

---

## 2. Порядок переносов

Порядок не косметический: перенос, после которого домен смотрит в инфраструктуру, **не
выполним** — `ArchitectureMapTests` покраснеет, и это единственная проверка в карте,
которая делает перенос невозможным, а не дорогим. Поэтому сначала независимые, потом
разрез, потом блок.

**Шаг 1. Две независимые группы в инфраструктуру.**
`IRgbController` + `RgbDevice`; группа LampArray (9 типов). Ни один доменный файл их не
называет, `IDevice` их не перечисляет, JSON и глоссарий не задеты. Дешёвле всего положить
их рядом с `LampArrayTransport.*.cs` (namespace `AcerHelper.Infrastructure.Vendors.Generic`,
тогда транспортам не нужен ни один `using`) либо в новую папку `Infrastructure/Lighting/` —
карте важно только одно: namespace обязан стать именем папки. **Здесь же оплачивается
счёт за `Application`**: `LaptopService` и `OptionsAssembler` начинают называть
инфраструктуру, и решение «узкий контракт вместо прямого типа» лучше принять на этом шаге,
а не после.

**Шаг 2. `AppArgs` → Application.** Решено владельцем, три файла, ничего не задет.

> **Сделано 2026-09-17.** `Application/AppArgs.cs`, namespace `AcerHelper.Application`; `using`
> заменён с `AcerHelper.Domain` на `AcerHelper.Application` в двух файлах автозапуска
> (`Autostart.Windows.cs`, `Autostart.Linux.cs`). `UI/App.axaml.cs` не правился — нужный `using`
> у него уже был, как и записано в §1 выше. Перенос, а не смена: все 923 существующих
> утверждения остались зелёными без правок.

**Шаг 3. Разрез расписания.** `ReapplyTrigger` → Application, `ModeAxisTable.Schedule` →
Application (одним типом или как член переехавшего). `ModeAxisTable` остаётся в домене с
`All`/`Policy`/`PolicyWhenNoModeWasEverConfigured`/`IsVolatile`/`Owner`. Правки: 1
продакшн-файл (`HardwareReconciler`) и 3 тестовых. **Глоссарий** держит имя
`ModeAxisTable` — имя остаётся, поэтому набор тестов зелёный; строка в `context-map.md`
требует правки только в колонке слоя.

> **Сделано 2026-09-17.** `ReapplyTrigger` переехал в `Application/HardwareReconciler.cs`, а
> `Schedule` стал `HardwareReconciler.Schedule(ReapplyTrigger)` (`internal static`) там же. Из двух
> вариантов имени, названных выше, выбран второй: таблица осталась одна и потеряла член, второй
> тип не заводился, потому что единственный исполнитель расписания и есть `HardwareReconciler`.
> Списки по-прежнему **собираются** из `All` и `IsVolatile`: расписание читает таблицу, а не
> повторяет её. Перенос, а не смена — те же триггеры дают те же оси в том же порядке, и все 923
> утверждения остались зелёными; в тестах правились только `using` и цель вызова.
>
> **Поправка к написанному выше: `context-map.md` не потребовал правок.** Строка `ModeAxisTable`
> в его §1 перечисляет «политику пресета нет, волатильность, кто переприменяет» и слой `Domain`,
> то есть после разреза описывает ровно то, что осталось. `ReapplyTrigger` по решению §3 «Слова
> без типа» намеренно без типа в первой колонке, поэтому колонки слоя у него нет и устареть
> нечему. `GlossaryTests` зелёный без правок.

**Шаг 4. Формы хранения в инфраструктуру.** `Settings`, `ISettingsStore`, `LightSettings`,
`LightPreset`, `GpuOcPreset`, `CoPreset`, `ProfileMemory` → `Infrastructure/Composition/`.
`ModeKey` — туда же (он ключ к этим словарям). **`FanPreset` остаётся**: его перенос
делает `Fan` зависимым от инфраструктуры, а `Fan` в домене по решению владельца.
**`SettingDeclaration` / `FlagSetting` / `ChoiceSetting` / `SettingNotAppliedException` и
`IFlagPort` / `IChoicePort` остаются** — контрактная половина. Побочный выигрыш: домен
перестаёт зависеть от `Localization`, и единственное исключение в `ArchitectureMapTests`
закрывается само (тест остаётся зелёным — он разрешает надмножество, — но его доккомментарий
надо привести в соответствие, иначе он описывает мир, которого больше нет).

**Шаг 5. CO-правила в инфраструктуру.** `CoAxis` + `OffsetCounts` → к
`RyzenCurveOptimizer.Windows.cs`. Выполнимо независимо: `VoltageDomain` и `CoPreset` можно
оставить на месте (инфраструктуре в домен можно), и `CoAxisTests` переезжает за типом.
`CoPreset` уезжает на шаге 4, потому что это форма хранения.

**Шаг 6. Один вопрос вместо десяти: `IDevice`.** Десять портов
(`IKeyboardBrightness`, `IHotkeys`, `IDisplayTint`, `IGpuOverclock`, `ICpuPower`,
`ICurveOptimizer`, `VoltageDomain`, `IDriverSetup`, `IAutostart`, `IClamshell`) переносятся
**только вместе с `IDevice`** — иначе домен смотрит в инфраструктуру. Пока ответа нет,
карта в этой части заморожена, и это надо считать одним решением, а не десятью.

**Что переносить не нужно вообще:** `Battery`/`BatteryToggle`/`BatteryChoice`, `Fan`,
`FanCurveEngine`, `ModeAxis`, `EmptyAxisPolicy`, `ReassertOwner`, `ModeAxisTable` (без
`Schedule`), 12 типов `Models.cs`, `IPowerProfiles`/`IFanControl`/`ISensors`, `RgbZone`,
`IRgbDevice`, четыре типа контракта настроек, `IFlagPort`/`IChoicePort`.

---

## 3. Спорные

### 3.1 `IDevice` — и вместе с ним весь вендорский блок портов

**Два прочтения.**

- **(A) `IDevice` — доменный контракт.** Домен объявляет, что ему нужно от машины;
  инфраструктура это реализует. Тогда `IDevice` остаётся, вендорские порты остаются
  вместе с ним, а `VoltageDomain`/`CoAxis` либо тоже остаются, либо переносятся частично.
  Это сегодняшняя форма и то, что говорит глоссарий (`context-map.md` ставит `IDevice` в
  Domain).
- **(B) `IDevice` — инвентарь возможностей конкретной машины.** «Набор ненулевых портов
  описывает ровно то, что умеет это сочетание вендор × ОС» — это описание **этой** машины,
  то есть та же категория, по которой владелец уже вынес `Settings` («опции, которые
  объявляет конкретно этот ноутбук»). Тогда `IDevice` и весь вендорский блок портов уезжают
  в инфраструктуру, домен остаётся без единого вендорского имени.

**Что различает чтения:** есть ли у домена объект, который **рассуждает** через
`IDevice`. Сегодня нет: `IDevice` только передаётся в `LaptopService`, а рассуждает
Application. Если это так и останется, чтение B честнее; если домен когда-нибудь начнёт
сам объявлять «вот контракт, который мне нужен от машины», верным станет A.

**Цена A:** `ICurveOptimizer`, `IGpuOverclock`, `ICpuPower`, `VoltageDomain` и ещё шесть
портов моделируют вендора и остаются в домене — то есть домен продолжает знать имена
технологий AMD, Nvidia и Windows, что и есть та ошибка, против которой написано правило.
**Цена B:** доменный интерфейс уходит из домена, а `IDevice.Battery` (домен) и
`IDevice.DeclaredSettings` (домен) остаются ссылками вниз — то есть инфраструктура
начинает описывать доменные объекты; 11 потребителей `IDevice` (Application, UI,
инфраструктура, 3 тестовых файла) получают новые `using`.

**Чего эта карта не делает:** она **не** переносит ни один из десяти портов. До ответа
любой такой перенос красит `ArchitectureMapTests`, то есть не выполним технически.

### 3.2 `OptionToggle`, `OptionChoice` — строка UI или DTO Application

**Два прочтения.**

- **(A) UI.** План сам называет `Domain/Options.cs` «контрактом строки UI» (§7.3), а
  подпись и локализация — по директиве владельца — принадлежат UI. Тогда тип уезжает в
  `UI/ViewModels/`.
- **(B) Application.** Эти записи — то, что **производит** `OptionsAssembler`, а он
  оставлен в Application осознанно. Тогда тип остаётся там, где собирается.

**Что различает чтения:** кто владеет формой строки — тот, кто её рисует, или тот, кто её
собирает. **Цена A:** `OptionsAssembler` (Application) начинает зависеть от UI, то есть
Application смотрит вверх — `ArchitectureMapTests` этого **не** запрещает, но весь смысл
карты слоёв в том, что это ошибка; лечится переносом `OptionsAssembler` в UI, а это уже
другая работа. **Цена B:** форма строки остаётся в слое, который её не показывает, и
UI-специфичные имена (`Confirm`, `Prime`, `Read`) живут ниже UI.

**Моё чтение: B**, потому что перенос по A тянет за собой ассемблер строк, а он —
оркестрация (Application по определению владельца). Но это ровно тот случай, где
уверенное неверное размещение уже случалось, поэтому пункт оставлен открытым, а не решён
молча.

### 3.3 `FanPreset` — форма хранения против модели `Fan`

Владелец вынес `Settings` (и вместе с ним форму хранения) в инфраструктуру. `FanPreset` —
форма хранения: замороженная схема, объект на режим, держит **оба** вентилятора. Но
`Fan.Duty(FanPreset, int)` и `FanCurveEngine.Step(FanPreset, SensorSnapshot)` — доменные, и
перенос пресета в инфраструктуру делает домен зависящим от неё, что не выполнимо.

**Два разрешения, и оба — решения владельца:**

- **(A) `FanPreset` остаётся в домене** как DTO замороженной схемы, на половину которого
  модель смотрит. Это ровно то, что уже записано про волну 3 («`Fan` — **вид** на свою
  половину `FanPreset`»), и это ничего не стоит.
- **(B) Сигнатура `Fan` меняется** на нейтральную кривую (анкоры + массив дежурств + флаг
  «кривая или фиксированная скорость»), и тогда пресет уезжает со всей остальной формой
  хранения. Это **смена контракта**, а не перенос: правятся `Fan`, `FanCurveEngine`,
  `LaptopService.Fans`, `FakeFanControl` и 4 тестовых файла, и в эту карту такая правка не
  входит по условию задачи.

**Моё чтение: A** — потому что правило владельца разрешило `Settings` как хранилище, а
`FanPreset` в её нынешнем виде является ещё и входом доменной модели, то есть выполняет
две роли сразу. Разделение этих ролей — отдельная работа с отдельной ценой.

### 3.4 `FanMode` — числа `1/2/3` под доменным именем

Перечисление нейтрально по смыслу, но его **значения** — это WMI-значения Acer, и
`FanPreset.Mode` персистит именно их. То есть кодировка вендора лежит в домене в виде
числа, а не в виде типа, — и страж на фикстуре её держит.

Это не вопрос слоя (словарь поведения вентилятора — домен), а **остаточная протечка
внутри** домена, и починка её означает либо маппер значений на границе, либо изменение
того, что лежит в `settings.json`. Второе запрещено условием задачи, первое — смена
контракта. Записано, чтобы не потерялось, а не как предложение.

### 3.5 `IFlagPort` / `IChoicePort` — если контрактная половина сменит форму

Оба остаются в домене **не по существу, а по привязке**: `FlagSetting.Port` и
`ChoiceSetting.Port` — часть контрактной половины `Settings`, которую владелец оставил в
домене. Если бы декларация держала не порт, а операции-делегаты — ровно так, как это
сделано у принятых владельцем `BatteryToggle`/`BatteryChoice` — оба порта уехали бы в
инфраструктуру без единой потери. Это альтернатива, а не рекомендация: она меняет форму
контракта, то есть выходит за рамки этой карты.

---

## 4. Внутридоменные нарушения, которые карта не чинит

Это не вопросы слоя, но они того же класса — «домен знает то, чего знать не должен» — и
попадают в `docs/domain-refactoring-plan.md` §7 как решения владельца, а не сюда.

1. **`Fan` знает, что он GPU-вентилятор.** Поле `private readonly bool _gpu` и
   конструктор `Fan(bool gpu)`. Владелец назвал это прямым текстом как ошибку, против
   которой правило и написано. Исправление — это **смена контракта** (выбор половины
   пресета передаёт вызывающий: `Fan.Duty(SensorSnapshot, …)` или явный селектор), а не
   перенос, поэтому в карту не входит. Пока это не исправлено, `Fan` остаётся доменным
   типом, который знает, какой он вентилятор.
2. **`RgbModeInfo.Handle`** — `object` с вендорской кодировкой (байт эффекта, флаг) внутри
   доменной записи. Домен его не читает и не интерпретирует, но и не может: он уходит
   обратно в бэкенд. Это тот же класс, что `FanMode`, и так же не чинится без смены формы.
3. **`AppInfo`** — тип в namespace `AcerHelper.Domain`, который под `Domain/` **не
   объявлен**: его генерирует цель `GenerateAppVersion` из `AcerHelper.csproj`. В этот
   разбор он не входит (список получен из файлов), но `EveryNamespaceNamesItsFolder` не
   может его увидеть, и его namespace — **второе место в дереве**, называющее слой. Если
   папка `Domain/` когда-нибудь переименуется, цель в csproj надо править вместе с ней.

---

## 5. Итог

| Слой | Типов | Из них переносимо сейчас |
|---|---|---|
| **Домен** | 32 | — (остаются) |
| **Application** | 2 типа + 1 член (`ModeAxisTable.Schedule`) | 3 |
| **Инфраструктура** | 32 | 21 |
| **Спорные** | 3 (`IDevice`, `OptionToggle`, `OptionChoice`) | 0 до ответа владельца |
| **Всего** | **69** | **24 из 69** |

Таблица считает то, что было **до** работ по §2. Шаги 2 и 3 сделаны 2026-09-17 (`AppArgs`,
`ReapplyTrigger` и член `Schedule` → `Application/`), поэтому «переносимо сейчас» — **21**,
а не 24.

Заблокировано: 10 портов гейтом `IDevice`, `FanPreset` — моделью `Fan`. Всё остальное —
перенос файла с namespace и `using`-ами; ни одно персистентное имя при этом не меняется,
потому что `settings.json` не видит имён типов.
