<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        x:Class="AcerHelper.UI.SidePanelWindow"
        Title="Acer Helper" Width="360" CanResize="False" ShowInTaskbar="False"
        WindowStartupLocation="Manual" WindowDecorations="None" Background="Transparent">

  <!-- Options/Lighting side panel: a separate acrylic window shown to the left of the main flyout.
       It is shown/hidden (no animation) and never moves once placed, so there's no resize ghosting
       or flicker, and it never overlaps the main window so nothing bleeds through the acrylic. -->
  <Border Classes="root">
    <DockPanel Margin="14">
      <Grid DockPanel.Dock="Top" ColumnDefinitions="*,Auto" Margin="0,0,0,10">
        <TextBlock x:Name="TitleText" Classes="h1" VerticalAlignment="Center"/>
        <!-- Close points toward the main window (to the right). Drawn as a vector so it's centred
             exactly (a "→" text glyph sits off-centre in its line box). -->
        <Button x:Name="BackButton" Grid.Column="1" Classes="icon">
          <Path Width="11" Height="10" Stretch="None"
                Stroke="{DynamicResource AppMuted}" StrokeThickness="1.6"
                StrokeLineCap="Round" StrokeJoin="Round"
                Data="M1,5 H10 M6.5,1.5 L10,5 L6.5,8.5"/>
        </Button>
      </Grid>
      <ScrollViewer HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Auto">
        <ContentControl x:Name="ContentHost"/>
      </ScrollViewer>
    </DockPanel>
  </Border>
</Window>
